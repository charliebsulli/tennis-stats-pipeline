sources:
  - name: raw
    schema: public
    tables:
      - name: raw_matches
        config:
          loaded_at_field: time_added
          freshness:
            warn_after:
              count: 24
              period: hour